<div class="header">
    <h1>Create A Round</h1>

    <p>Answer: The user's response must match the answer exactly to be counted as correct. The answers are not case sensitive.
    <br>Keywords: If the user's response contains all keywords, the response will be counted as correct. Separate keywords by spaces. Do not use commas.</p>
</div>

<% if @errors %>
    <ul>
    <% @errors.each do |error| %>
        <li><%= error %></li>
    <% end %>
    </ul>
<% end %>

<%= render partial: 'form', locals: {round: @round, current_user: @current_user} %>