<h1><%= @game.name %></h1>

<ol>
<% @game.rounds.each do |round| %>
    <li><%= round.name %></li>
<% end %>
</ol>

<%= link_to "Start Next Round", new_round_round_play_path(@current_game_round.round) %>